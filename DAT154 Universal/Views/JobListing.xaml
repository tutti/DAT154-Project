<Page
    x:Class="DAT154_Universal.Views.JobListing"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DAT154_Universal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ComboBox x:Name="job_type_list" Height="30">
            <ComboBoxItem Name="maintainer">Cleaner</ComboBoxItem>
            <ComboBoxItem Name="cleaner">Service</ComboBoxItem>
            <ComboBoxItem Name="service">Maintainer</ComboBoxItem>
        </ComboBox>
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">
            <TextBlock Text="Jobs available for *insert jobtype here*" FontSize="21" TextAlignment="Center"/>
            <ListView Name="TaskList">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <!--<TextBlock Text="{Binding room.room_number}"/>-->
                            <TextBlock Text="{Binding status}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
